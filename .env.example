# Required
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Polling interval in seconds (default: 3600 = 1 hour). Use 0 to run once and exit.
POLL_INTERVAL_SECONDS=3600

# Reviews older than this many days are ignored (default: 90). Use 0 to disable the filter.
MAX_REVIEW_AGE_DAYS=90

# Apple App Store
# Set APPLE_ENABLED=false to disable without removing the other vars.
APPLE_ENABLED=true
APPLE_APP_ID=          # numeric ID from the App Store URL: .../id564924168
APPLE_REGION=br        # br, us, pt, es, ...

# Google Play Store
# Set PLAY_STORE_ENABLED=false to disable without removing the other vars.
PLAY_STORE_ENABLED=true
PLAY_STORE_PACKAGE=    # package name, e.g. com.example.app
PLAY_STORE_LANG=pt     # pt, en, es, ...
PLAY_STORE_COUNTRY=br  # br, us, ...

